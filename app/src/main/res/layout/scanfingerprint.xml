<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF"
    tools:context=".ScanFinger">


    <TextView
        android:id="@+id/headingLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:padding="20dp"
        android:text="Fingerprint Authentication"
        android:textColor="#000000"
        android:textSize="24sp" />

    <ImageView
        android:id="@+id/fingerprintImage"
        android:layout_width="72dp"
        android:layout_height="72dp"
        android:visibility="invisible"
        android:layout_below="@+id/headingLabel"
        android:layout_centerHorizontal="true"
        app:srcCompat="@mipmap/app_logo_round" />

    <TextView
        android:id="@+id/paraLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/fingerprintImage"
        android:layout_centerHorizontal="true"
        android:padding="20dp"
        android:text="Place your Finger on the Scanner to Proceed!"
        android:textAlignment="center"
        android:textColor="#000000" />


    <ImageView
        android:id="@+id/statusgreenfingerprint"
        android:layout_width="72dp"
        android:layout_height="72dp"
        android:layout_below="@+id/headingLabel"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="2dp"
        app:srcCompat="@drawable/greenfinger"/>

    <ImageView
        android:id="@+id/statusredfingerprint"
        android:layout_width="96dp"
        android:layout_height="88dp"
        android:visibility="invisible"
        android:layout_alignWithParentIfMissing="false"
        android:layout_below="@+id/headingLabel"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="-4dp"
        android:background="#FFFFFF"
        android:duplicateParentState="false"
        app:srcCompat="@drawable/redfinger" />
</RelativeLayout>